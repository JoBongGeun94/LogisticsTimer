export const TIMER_CONFIG = {
  UPDATE_INTERVAL: 10,
  PRECISION_DIGITS: 2,
} as const;

export const KEYBOARD_SHORTCUTS = {
  TOGGLE_TIMER: 'Space',
  RECORD_LAP: 'Enter',
  STOP_TIMER: 'Escape',
  RESET_TIMER: 'KeyR',
} as const;
